import "package:flutter/material.dart";

class StatsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
      return Center(child: Text("Stats", style: TextStyle(fontSize: 20.0)));
  }
}
